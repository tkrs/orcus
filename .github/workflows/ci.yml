name: CI
on:
  push:
    branches:
      - master
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        scala: 
          - "2"
          - "3"
    steps:
      - uses: actions/checkout@v4
      - id: scala-version
        run: |
          scala_version=$(grep "scala${{ matrix.scala }}" project/Dependencies.scala | sed -E 's/.*= //')
          echo "value=${scala_version}" >> $GITHUB_OUTPUT
      - uses: actions/setup-java@v4
        with:
          java-version: "11"
          distribution: "temurin"
          cache: "sbt"
      - uses: sbt/setup-sbt@v1
      - run: sbt ++${{ steps.scala-version.outputs.value }} test
        if: matrix.scala == '3'
      - run: sbt ++${{ matrix.scalaVersion }} coverage test coverageReport
        if: matrix.scala == '3'
      - uses: codecov/codecov-action@v5
        if: matrix.scala == '2'
        with:
          flags: unittests
          verbose: true
  scalafmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: "11"
          distribution: "temurin"
          cache: "sbt"
      - uses: sbt/setup-sbt@v1
      - run: sbt scalafmtCheckAll
  scalafix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: "11"
          distribution: "temurin"
          cache: "sbt"
      - uses: sbt/setup-sbt@v1
      - run: sbt "scalafixAll --check"
